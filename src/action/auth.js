import {
    REGISTER_SUCCESS,
    LOGIN_SUCCESS,
    LOGIN_FAIL,
    REGISTER_FAIL,
    LOAD_USER,
    AUTH_ERROR

} from '../constants/constants'

export const loadUser = () => async dispatch => {
    try {
        
        

    } catch (error) {
        
    }
}